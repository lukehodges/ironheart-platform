import { inngest } from "@/shared/inngest";

/**
 * Team Inngest functions.
 *
 * Stub for Phase 5 — full team invite flow (email notifications, onboarding
 * sequences) will be implemented in a future phase once the notification
 * module's team-specific triggers are defined.
 *
 * All team Inngest functions — register in src/app/api/inngest/route.ts
 */
export const teamFunctions: ReturnType<typeof inngest.createFunction>[] = [];
