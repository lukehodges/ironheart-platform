import { PlaceholderPage } from "@/components/platform/placeholder-page"

export default function NewClientPage() {
  return (
    <PlaceholderPage
      section="Platform / Clients / New"
      title="New Client"
      subtitle="Create a new client engagement (built in Phase 0.1.B)."
    />
  )
}
