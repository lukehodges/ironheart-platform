import { PlaceholderPage } from "@/components/platform/placeholder-page"

export default function BookingsPage() {
  return (
    <PlaceholderPage
      section="Platform / Bookings"
      title="Bookings"
      subtitle="All calls + sessions across clients."
    />
  )
}
