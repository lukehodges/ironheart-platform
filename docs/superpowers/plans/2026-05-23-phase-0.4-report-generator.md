# Phase 0.4 — Report Generator

> **Parent roadmap**: [`ROADMAP.md`](./ROADMAP.md)
> **Status**: 📋 PLANNED
> **Depends on**: Phase 0.3 (audit data complete)
> **Estimate placeholder**: ~4 days

---

## Goal

Take completed audit data (5 lenses, findings, recommendations, form responses, call notes) → Claude API drafts a full audit report → Luke edits in rich text editor → publishes to client portal.

Existing module: `src/modules/report-generator/` (per migration doc) — verify exists, build out.

---

## Sub-phases

### 0.4.A — Backend AI draft

- [ ] Schema: `auditReports` table per Phase 3A spec (verify exists)
- [ ] Claude SDK integration in `src/modules/ai/` (exists)
- [ ] Prompt template that takes structured audit data → returns sectioned report (exec summary, per-lens analysis, roadmap)
- [ ] Inngest function `report/generate` w/ Claude streaming
- [ ] Status: GENERATING → DRAFT → IN_REVIEW → PUBLISHED
- [ ] **Prompt caching** for the system prompt + audit data context (per `claude-api` skill best practice)

### 0.4.B — Editor UI

- [ ] `/platform/clients/[id]/report` page
- [ ] Side-by-side: structured audit data left (read-only), report editor right
- [ ] Rich text editor (TipTap or similar, check existing stack)
- [ ] "Regenerate section" button per section
- [ ] Internal review checklist CL-03 (loaded from `the-ironheart-ltd/SOPs/Checklists/CL-03-Report-Review.html`)
- [ ] "Publish to client" CTA → moves status, fires `report/published` event

### 0.4.C — PDF + Drive

- [ ] HTML → PDF via Puppeteer or @react-pdf/renderer
- [ ] Branded template w/ Ironheart design tokens
- [ ] Upload PDF to engagement Drive folder (Phase 5 stub — for 0.4 store in own filestore/Vercel Blob)
- [ ] Download PDF from report editor

---

## Open questions

1. Editor library — TipTap vs Lexical vs plain markdown? Check `package.json`.
2. Claude model — `claude-opus-4-7` for first drafts? Cost vs quality.
3. PDF rendering — Puppeteer locally or hosted (e.g. browserless.io)?
4. Drive storage — defer to Phase 1.x or include here?
5. Streaming UI — show generation progressively or wait for full draft?

## Success criteria

- Luke clicks "Generate Report" from audit workspace
- Draft appears within 60s w/ all 5 lens sections + exec summary
- Edits autosave
- "Publish" makes report visible on client portal
- PDF downloadable
