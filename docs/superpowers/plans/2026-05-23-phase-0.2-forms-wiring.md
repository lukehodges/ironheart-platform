# Phase 0.2 — Forms Wiring + Portal Audit Tab

> **Parent roadmap**: [`ROADMAP.md`](./ROADMAP.md)
> **Status**: 📋 PLANNED — flesh out when 0.1 lands
> **Depends on**: Phase 0.1 (chart + tenant + shell complete)
> **Estimate placeholder**: ~3 sub-phases

---

## Goal

Turn the org chart into actual form sends. Seed templates from existing HTML questionnaires. Wire `enqueueOnboardingForms` to fire real `formsService.sendForm` calls. Surface completion progress in `/[tenantSlug]/dashboard/audit`.

After 0.2, Luke can stop using Tally. Every audit's questionnaires live inside the platform.

---

## Sub-phases (stub — refine before starting)

### 0.2.A — Form template seeds (~1 day)

- [ ] Parse 6 questionnaires in `the-ironheart-ltd/Templates/Questionnaires/*.html`
- [ ] Map HTML field types → `FormField[]` (`forms.types.ts`)
- [ ] Drizzle seed script `scripts/seed-questionnaires.ts`
- [ ] Tag each template w/ `audience` metadata (owner/ops/finance/sales/team/quick)
- [ ] Manual verify via existing `/forms/[sessionKey]` public route
- [ ] Resolve D-07: include quick-pulse template or not

### 0.2.B — Chart → Forms enqueue (~2 days)

- [ ] Wire `onboarding.service.enqueueOnboardingForms` to call `formsService.sendForm` per planned send
- [ ] Inngest handler for `engagement/onboarding-plan-approved` → fires sends
- [ ] Track sent invitations in `engagement_org_chart` node metadata or new `org_chart_form_send` table
- [ ] Resend logic when client adds person to SAMPLE slot post-approval
- [ ] Tests: unfilled slots warn but don't block, idempotent on re-approval

### 0.2.C — Portal audit progress tab (~2 days)

- [ ] `/[tenantSlug]/dashboard/audit` route
- [ ] Form completion checklist per recipient (read `completedForms` by tenant)
- [ ] Status badges: PENDING / SENT / IN_PROGRESS / COMPLETED
- [ ] Cal.com upcoming sessions list (existing `booking` module)
- [ ] Activity feed scoped to current engagement
- [ ] No findings/RAG visible yet — gated until report published (D-04 implicit)

---

## Open questions (resolve before starting)

1. When `mode=SAMPLE(N)` dept has 0 named people, does enqueueOnboardingForms send anything for that dept? Probably no — wait for client to fill names.
2. Reminder emails for forms not started after N days — in 0.2 or 0.3?
3. Sample slot UX on client side — "fill 3 slots" task card on portal landing?

## Success criteria

- Approving onboarding plan in `/platform/clients/[id]/onboarding` fires real emails via Resend
- Recipients click link, complete questionnaire, see completion in portal
- Luke sees "5/8 forms completed" stat on his engagement detail
- Client owner sees same stat on their portal landing
